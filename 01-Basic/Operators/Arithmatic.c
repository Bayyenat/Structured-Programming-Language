#include<stdio.h>
int main() {
    int x= 22;
    int y= 7;


    printf("%d is the sum\n", x+y);
    printf("%d is the neg value\n", x-y);
    printf("%d is the product\n", x*y);
    printf("%d is the vagfol\n", x/y);
    printf("%d is the vagsesh\n", x%y);

    printf("%d is the next value of x\n", x++);
    printf("%d is the next value of y", y--);
    return 0;
}
